export interface LoginResponse {
  statusCode: number;
  status: string;
  message: string;
  data: string;
}

export interface RegisterResponse {
  statusCode: number;
  status: string;
  message: string;
}
